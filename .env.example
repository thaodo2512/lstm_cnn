# Copy this file to .env and adjust values as needed.
# Docker Compose automatically loads .env from the project root.

# --- Training (hybrid_lstm_transformer_crypto.py) ---
EPOCHS=10
HORIZON=1
BATCH_SIZE=32
WINDOW_SIZE=60
STRIDE=1
SOURCE=auto
CLASSIFICATION=false

# --- FreqAI backtesting / signal bot common ---
# Comma-separated list of pairs
PAIRS=BTC/USDT:USDT,ETH/USDT:USDT
# Comma-separated timeframes to pre-download for FreqAI/strategy
TIMEFRAMES=5m,15m,30m,1h,2h,4h
# Backtest timerange
TIMERANGE=20250101-20250930
# Toggle strategy protections during backtesting (--enable-protections)
ENABLE_PROTECTIONS=false
# Model class used by FreqAI
FREQAIMODEL=HybridTimeseriesFreqAIModel_tinhn
# Strategy class name (e.g., ichiV1 or FreqAIHybridImproved5mShort)
STRATEGY=ichiV1

# Output type for backtesting (trades|signals)
EXPORT_TYPE=trades

# Download in blocks to ensure enough prehistory for FreqAI
BLOCKS=1
BLOCK_DAYS=30

# --- ichiV1 strategy tunables (can adjust without code changes) ---
# Target threshold: mean + ICHI_STD_MULT * std
ICHI_STD_MULT=0.5
# Bullish EMA level required (1..8)
ICHI_BULLISH_LEVEL=2
# Above-cloud strictness (0 disables cloud gate)
ICHI_CLOUD_LEVEL=1
# Consecutive fan magnitude increases required
ICHI_FAN_SHIFT=1
# Minimum fan magnitude gain
ICHI_FAN_GAIN=1.001
# Require FreqAI do_predict mask
ICHI_REQUIRE_DOPREDICT=true
# Use Heikin Ashi pre-processing
ICHI_USE_HA=true
# Enable short signals (requires futures)
ICHI_ENABLE_SHORT=false
# Minimal gates to force trades for validation
ICHI_TEST_LOOSE=false
ICHI_TEST_THR=0.0
# Sell trend indicator to use for exits (must be a key in strategy.trend_period_map)
ICHI_SELL_TREND=trend_close_2h

# --- Telegram notifications (optional) ---
NOTIFY_TELEGRAM=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Web UI ---
TIMEFRAMES_WEBUI=1h,5m
